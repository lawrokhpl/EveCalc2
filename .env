# Copy this file to `.env` and adjust values as needed

# --- Backend selection ---
# file | sql
DATA_BACKEND=sql

# Optional local SQLite fallback (for development only)
SQLITE_PATH=data/local.db

# --- Postgres / Cloud SQL ---
# Provide either DB_HOST for TCP or CLOUD_SQL_CONNECTION_NAME for unix socket
DB_USER=lawrokhdb2025
DB_PASS=Papwpepl12!?BazaDanych
DB_NAME=EveCalcDB

# TCP connection (local/dev). Example: 127.0.0.1:5432
DB_HOST=

# Cloud SQL connection name (proj:region:instance). Leave DB_HOST empty when using this
CLOUD_SQL_CONNECTION_NAME=graphite-argon-454919-k2:us-central1:lawrokhdb2025

# --- Authentication backend ---
# local | google
AUTH_BACKEND=local

# Google OAuth 2.0 (fill only if AUTH_BACKEND=google)
OAUTH_CLIENT_ID=
OAUTH_CLIENT_SECRET=
OAUTH_REDIRECT_URI=

# --- Streamlit ---
STREAMLIT_SERVER_HEADLESS=true
STREAMLIT_SERVER_PORT=8080